/**
 * Pos2D Class
 * Used to store a 2D position
 */
export class Pos2D {
    x: number | undefined;
    y: number | undefined;

    constructor(x: number | undefined, y: number | undefined) {
        this.x = x;
        this.y = y;
    }
}